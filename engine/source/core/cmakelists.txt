set(project_dir "${CMAKE_CURRENT_LIST_DIR}")

file(GLOB_RECURSE project_files *.hpp *.cpp)

if(shared)
    add_library(Core SHARED ${project_files})
else()
    add_library(Core STATIC ${project_files})
endif()

#find_package(spdlog)

if(spdlog_FOUND)
    target_include_directories(Core PRIVATE ${spdlog_INCLUDE_DIR})
    target_link_libraries(Core ${spdlog_LIBRARY})
endif()

#set_precompiled_header(Core core/precompiled.hpp)
#set_precompiled_header(Core core/precompiled_core.hpp)

add_compile_definitions(ENGINE_ROOT_PATH="${PROJECT_ABSOLUTE_SOURCE_DIR}")

source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${project_files})